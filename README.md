In dit project heb ik een layout van een pagina gemaakt. De opdracht van deze pagina was als volgt:
- Make a HTML page according to the picture (see Canvas) [HTML + CSS]
- Try to use the correct semantic HTML elements [HTML]
- Use Flexbox (1D) and Grid (2D) for layout [CSS]
- Put this on your HERA-hosting (if that still works) and send Maikel Putman (DEV teacher) the link (on MS Teams) [Selfservice portal]
- Optional: Make the header bar move with the page (fixed) [CSS]
- Optional: Make it responsive (with media-queries) [CSS]
- Optional: Use/practice Git to track your work [Git]

